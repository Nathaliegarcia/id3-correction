[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "id3-correction"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["mutagen"]

[project.scripts]
id3-correction = "id3_correction.__main__:main"
